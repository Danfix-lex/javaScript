const names = ["Emily", "Jack", "Sophia", "Daniel"];
const books = ["English", "Math", "Yoruba", "Biology"];

function bookClub(names, books) {
    let students = [];
    for (let i = 0; i < names.length; i++) {
        students, <map name=""></map>
    }
    return students;
}

console.log(bookClub(names, books));